
  # Student Dashboard UI Spec

  This is a code bundle for Student Dashboard UI Spec. The original project is available at https://www.figma.com/design/TbvwIHXM0NCocGRgapJy68/Student-Dashboard-UI-Spec.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  